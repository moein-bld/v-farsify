name: auto-format

on:
  pull_request: null
  
jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout the branch
        uses: actions/checkout@v4
      - name: Run linter check
        run: echo "do your format checking here"
